allOf:
  - type: object
    properties:
      process:
        type: string
        format: uri
        description: The URI to the description of the process that should be executed.
  - $ref: "responseCollection.yaml"
  - $ref: "executeBase.yaml"
